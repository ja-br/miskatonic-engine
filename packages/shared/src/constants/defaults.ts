/**
 * Default configuration constants
 */
export const DEFAULT_WINDOW_SIZE = {
  width: 1280,
  height: 720,
  minWidth: 800,
  minHeight: 600,
} as const;

export const DEFAULT_IPC_TIMEOUT = 5000; // 5 seconds

export const DEFAULT_LOG_LEVEL = 'info' as const;
